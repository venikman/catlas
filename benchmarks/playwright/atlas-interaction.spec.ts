// Optional interaction benchmark spec placeholder.
//
// When @playwright/test is added, this spec should measure:
// - pan p95 frame time
// - zoom p95 frame time
// - hover feedback latency
// - click-to-side-panel latency
// - search response and fly-to behavior
// - stale request cancellation/ignoring during repeated pan/zoom cycles

export {};
