// Optional visual sanity spec placeholder.
//
// When @playwright/test is added, capture low/medium/high zoom screenshots and
// compare only stable, high-level assertions first:
// - nonblank atlas canvas
// - visible density layer at low zoom
// - visible clusters/labels at medium zoom
// - visible bounded points at high zoom
// Avoid brittle pixel-perfect assertions until the visual polish phase settles.

export {};
